# Cardinal build log — July 26, 2026 session (298–308)

- **298** — Chrome rebuild: header back to `position:fixed`, ribbon fused beneath (clock / PO+name via `body.projopen`), per-CRM ribbon palettes, floating banners deleted at source; duplicate `cr-bulk` id untangled (Bulk Portal Assign → `CardinalPortalAssign`).
- **299 / 299b** — Frost community home: old `cr-ch2` replaced wholesale (data core + plumbing preserved verbatim), three tabs, partner-type grouping via `select('name,partner_type')`, role gating (admin totals / sales own / production none). Header+ribbon community vars → Frost. RLS v3 migration run (estimates+punch scoped transitively; line-items exempted as the price book; `punch_open_counts` security_invoker). Anon probe: zero rows everywhere.
- **300** — Tab buttons un-hidden (direct-child pane scoping; the preview bug re-typed into the module), phantom ＋ root-caused (`#addProjectBtn{font-size:0}` + `::after` legacy pair deleted), six legacy title tints deleted, bar nowrap, build label finally bumped.
- **301** — One header everywhere: four-control bar with screen-name titles, gold home in the ribbon, shield+switcher to the bottom bar, `cr-ih` stubbed, phoenix headers retired, insurance views under the chrome, community masthead removed, footer tagline hidden on community, chip's CT-deference guard removed.
- **302** — Insurance views drop to z-60 (menus/sheets open on top), legacy button-sizing rules deleted, titles bigger/centered/gradient per CRM, New Bid modal above the nav with fixed grid columns. **Regression shipped:** literal `\1` backreferences destroyed five skin rules.
- **303** — The five skin rules reconstructed verbatim (gates now assert whole rules), title lowered onto the controls row, home aligned to the burger, retail dark-iron ground with `--red`→gold remap.
- **304** — 157 stylesheet literal reds → `var(--red)` (5 defs protected, cycle-checked), 3 inline dots converted, Client Projects box back to white, gold card borders, gradient-gold quote + acthead, title 26px, home button 38px, ribbon 52px.
- **305** — Community client page gains Job Menu + Location: proxy grid + live-map adoption with stash-and-return lifecycle (gate caught a missed release on the observer exit path — fixed), retail `projsec` headings gradient-gold, last 18 inline `color:` reds converted.
- **306** — Community client fully Frost: 12 legacy `body.claim-*` header rules deleted (the mint-header cause), takeover becomes a direct-child nuke with dark ground + footer hide, palette values swapped to Frost, floating "bid" label removed, job-menu retry, Google Reviews adopted (dual-adoption lifecycle proven).
- **307** — Job menu proxies the REAL `.jatile` grid with `.jn` counts and delegated clicks (`.jabox` was a theming class; the previous harness had validated the author's own fiction — gate now replicates the real builder).
- **308** — Full bug battery: 830-id phantom audit (25 = guarded/dynamic, none live), duplicate ids resolved as iframe-scoped, `!important` down to 250, zero orphaned handlers. **One live bug found and fixed:** `CardinalEstimates` overwritten by a second module, nine AI-estimate call sites dead → both assignments now `Object.assign` merges; dupe-API check added to the standing gates. Runtime walk: 27 menu options, all community flows, 11 API calls, zero real console errors; `bid_due_at` countdown proven end to end.
- **309** — Old landing never paints (children hidden until the hub stamps `data-cr-portal-built`; legacy ids kept for boot writers — true markup deletion still queued). Cardinal Truth and Community flipped to theme-true colors on landing rows and switcher cards; Landing/Hub card gets its own warm-gold rules (it had none — the white was the unstyled default).
- **310** — Self Check's Cardinal Claims section remapped from the retired phoenix bar to the live chrome (banner home, bottom-bar switcher, global + and ☰). On-device audit otherwise fully green.
- **311** — Dark canvas behind Community (`#0a1420`) and Claims (`#08161a`) kills the white rubber-band overscroll; AccuLynx import pop-up restyled to dark iron with a gold header.
- **312** — Import option boxes corrected to white cards with black lettering per Theo; header titles up to clamp(25px,7.2vw,32px) so they carry the bar.
- **313** — In-app What's New: ten user-facing entries covering 298–312 prepended to the changelog.
- **314** — "Where did my manual estimates go": the AI Estimates surface (revived by 308's merge) had a New Manual Estimate button whose handler was a developer stub pointing at the long-deleted duplicate feature. Wired to a searchable client picker feeding `CardinalEstimates.openEditor`.
- **315** — Both flashes killed at the root: the login→landing hop showed white mainView (309 had fixed a frame nobody saw); the orphaned `restoreVeil` — dark, z-500, zero main-document controllers — now rises first thing in `showMain` (1.6s failsafe) and drops when the landing paints. Community clients pre-take with a Frost skeleton before their network loads. Gate lesson: jsdom completes the hop synchronously, so both edges were proven by freezing the route mid-flight.
- **316** — `openEditor(project, existing)` wants the project OBJECT; Price-it and the picker passed an id string → "Untitled client". The function now hydrates from `cacheProjects` itself; name + address fill in. Editor sway contained (`overflow-x` + `min-width:0` on inputs); leads-list headings gradient-gold on iron; PO numbers lavender via a `.ljpo` span.
- **317** — Contracts subnote white on retail; saved-estimate rows sit in an iron inset so their rounding shows against the white panel.
- **318** — Estimate editor cleared only 40px past the bottom nav; now 150px + safe-area with `overscroll-behavior-y:contain`.
- **319** — Client head card tightened blind from a cropped screenshot. **Shipped worse.** Kept in the log as the lesson.
- **320** — The real fix, from real recon: `.projinfo h2` carried 150–170px right-padding reserved for `projTopBtns` that another rule hides with `display:none !important` — dead layout serving dead controls, wrapping every name. Deleted at source; the fixed 88px cover became stretch-capable; 319's band-aid replaced.
- **321/322** — The title saga's end: three timid clamp bumps (23→28→34px effective) had each been real but imperceptible — the user asked five times because the increments were invisible. Diagnosed the full cascade (the edited rule WAS the winner all along), previewed four sizes in a standalone mock of the real bar, user picked A → **fixed 34px**, lane widened to 70%. Doctrine reaffirmed: preview visual changes before shipping, and prefer fixed values over viewport math for approved sizes.
- **323** — What's New updated through the day.
- **324** — Estimates/Recents/Inspections list view (one shared renderer): gradient-gold heading, white note, gold Salesman label, `table.reports` and its thead/tbody made transparent so iron shows between the rows.
- **325** — **The Attach bar was never missing.** `#cr-pae-actionbar` sat at `bottom:0;z-index:80` beneath `#pwaNav` (z-9990) since the nav was introduced — every photo-pick session had a working Attach button buried under the RETAIL pill. Lifted above the nav; album lettering white.
- **326** — **Community tab-tiles fixed** (the roadmap's #1 gap): suspend-and-return. Tiles that open base *tabs* (Estimates/Contracts/Inspections/Materials/Checklists) release adopted nodes, drop the takeover, hide `#cr-cc` and raise a "❄ Back to bid view" pill; the pill re-renders and re-adopts. Suspension is anchored to the captured project id — anchoring it to `loadedFor` let the observer un-suspend instantly (gate caught it). Communications view wears Frost on community.
- **327** — **ABC Supply integration.** `api/abc.js` proxy (OAuth client-credentials per their published spec, 25-min token cache, actions for search/pricing/availability/frequents/templates/branches/orders) plus the in-app `cr-abc` sheet: connection status, Ship-To/Bill-To/Branch settings, catalog search, per-item branch pricing with the documented $0 "branch will price" case handled. Ships dark until credentials exist.
- **328** — **Community Analytics** (`cr-can`, admin-gated): win rate, awarded, out-for-pricing, average bid; partner table with win %; aging ladder for open bids (hot past 7 days); awarded-by-month bars. Placed as a tools tile, not a fourth tab.
- **329** — Activity and Calendar tiles wired to the app's **existing** feed and schedule board — no duplicate surfaces built.
- **330** — **The $0 rollup.** Community money read only `checklist.lead.bid_amount`; jobs won through an accepted estimate keep their total in `estimates`, so "Get on the calendar" showed rows worth $0. `bidAmt()` now falls back to a fetched map of estimate totals (accepted preferred over newer drafts). **First attempt staged a stale file with none of the fix in it and the gate passed anyway** — it was matching an unrelated estimates query elsewhere. Caught by inspecting the artifact; gates now run a **negative control** against the previous build.
- **331** — Claim-type stamping: the New Lead form always reset to "Don't know yet," so leads created from Community or Claims were typeless. It now pre-selects from `crmNow()` (Production/Sales still yield "unknown", correctly). Negative control proves v330 fails all three cases.
- **332** — Eight dead back-button handlers from retired layouts deleted; `galBackBtn`, `commsBackBtn` and six others verified live and untouched.
